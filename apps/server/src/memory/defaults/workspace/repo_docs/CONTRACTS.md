# Contracts

This file records durable public/internal contracts that future changes must preserve.

## Public Interfaces

- API routes:
- WebSocket events:
- Tool inputs/outputs:
- CLI or package entry points:

## Internal Contracts

- Cross-package boundaries:
- Persistence invariants:
- UI/runtime expectations:
- Error and recovery behavior:

## Update This When

- A schema, event, tool, API, command, or storage invariant changes.
- Frontend/backend responsibilities move.
- A bug reveals an implicit contract that should be explicit.
