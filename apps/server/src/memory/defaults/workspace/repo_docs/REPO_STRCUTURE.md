# Repo Structure

This file records the durable structure of this workspace.

## Source Of Truth

- Keep the current directory/module map and ownership boundaries here.
- Prefer the smallest useful map over a complete file listing.
- Update this when important folders or responsibilities change.

## Top-Level Layout

- `src/`:
- `app/`:
- `packages/`:
- `tests/`:
- `docs/`:

## Ownership Boundaries

- UI code:
- Backend/server code:
- Shared contracts/types:
- Tests:
- Generated or ignored output:

## Update This When

- A major folder/module is added, moved, or removed.
- Responsibility shifts between packages.
- A future agent needs a better starting map.
