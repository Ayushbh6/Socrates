{
  "name": "socrates",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.1",
  "scripts": {
    "build": "pnpm -r build",
    "desktop:bundle": "pnpm --filter @socrates/desktop bundle",
    "desktop:bundle:mac": "pnpm --filter @socrates/desktop bundle:mac",
    "desktop:bundle:windows": "pnpm --filter @socrates/desktop bundle:windows",
    "desktop:runtime": "pnpm --filter @socrates/desktop build:runtime",
    "runtime:archive": "pnpm --filter @socrates/desktop runtime:archive",
    "cli:test": "pnpm --filter @socrates-ai/cli test",
    "desktop:release:local": "pnpm --filter @socrates/desktop release:local",
    "desktop:dev": "pnpm --filter @socrates/desktop dev",
    "eval:context-compression": "pnpm --filter @socrates/shared build && pnpm --filter @socrates/contracts build && pnpm --filter @socrates/providers build && pnpm --filter @socrates/core build && node scripts/context-compression-eval.mjs",
  "eval:cost-efficiency": "pnpm --filter @socrates/shared build && pnpm --filter @socrates/contracts build && pnpm --filter @socrates/providers build && node scripts/cost-efficiency-eval.mjs",
    "trace:e2e": "pnpm --filter @socrates/server exec tsx ../../scripts/trace-retrieve-e2e.ts",
    "test": "pnpm -r --workspace-concurrency=1 test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}
